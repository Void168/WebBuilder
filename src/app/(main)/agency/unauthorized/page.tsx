import React from "react";
import Unauthorized from "@/components/unauthorized";

type Props = {};

const Page = (props: Props) => {
  return (
    <div>
      <Unauthorized />
    </div>
  );
};

export default Page;
